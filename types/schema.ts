export interface WebSiteSchema {
  name: string;
  url: string;
  description: string;
  priceRange: string;
  telephone: string;
  addressLocality: string;
  addressRegion: string;
  postalCode: string;
  addressCountry: string;
}